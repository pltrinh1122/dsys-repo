# DR-CMD-020 — `/eval-pb` worked reference: dsys-mutation-playbook (ratified, contingent)

- **Status:** ratified
- **Date:** 2026-09-21 ~04:44 PDT
- **Selector:** Peter (operator; proposer = ambient agent — proposer≠disposer held)
- **Disposition mode:** ratify
- **Matter:** Which existing playbook should be made `/eval-pb`'s exemplar. This is the new matter contemplated by DR-CMD-011's G6 ("If a qualified exemplar playbook ever exists *and* comparative calibration is wanted, that is a new matter — the door is not welded shut"). "Exemplar" here means **worked reference** (consultative: grounds the validator's judgment calls), not a comparative axis and not a replacement standard.

## Options and gate trails (from `/pb-decide` START/STOP)

- **O1 — §8.2 decision-making playbook:** killed. Circular — the definition `/eval-pb` validates against cannot serve as its own exemplar (the vacuity `/eval-sc` refuses as self-comparison); fails the validator's own dimension 7 per DR-CMD-011's falsification; and it is the pattern, not an instance.
- **O2 — dsys-mutation-playbook (§8.4):** survived; selected, **contingent** — exemplar status earned by passing `/eval-pb`, not granted by fiat. Ratified 2026-09-19; an instance on the §8.2 pattern (no circularity); golden run 37 refusal cases PASS (strongest dim-7 evidence of any candidate); own gates G1–G4+M1–M4; shared DecisionRecord discriminated by `playbook`; named disposition subset.
- **O3 — feature-expansion playbook:** deferred, not killed. DRAFT, unratified, unexercised (audit: dims 3 + 7). A standard cannot be a draft. Revisit trigger: after its bootstrap — ratified *and* admit/kill paths exercised. On trigger it re-enters as a candidate; the question then becomes one reference or two.
- **O4 — dsys-transcription-playbook:** killed. Specified, not ratified; rides the dialog protocol's exploratory status. May return reframed if ever ratified.
- **O5 — decide nothing:** survived as the null. DR-CMD-011's definition-anchored validation demonstrably works (the pre-ratification audit used it). Not selected: the positive case for a consultative reference — reviewer calibration plus instrument pressure-testing — is real and now executable.

## The contingency, executed

The ratified O2 authorized the `/eval-pb` run first. Executed in-chat 2026-09-21 per `doc/eval-pb-implementation.md`:

> Candidate `doc/dsys-mutation-playbook-spec.md` @ `b3058bb` (working tree, clean) — admitted (claims the playbook form: DoD conditionals, rung options, G1–G4+M1–M4 gates, proposer/disposer, disposition production). All ten dimensions conform (see report below). **Overall: conforms.**

Contingency satisfied: the dsys-mutation-playbook is thereby named `/eval-pb`'s worked reference.

## Reconciliation with DR-CMD-011

DR-CMD-011 killed the *comparative* exemplar (candidate↔exemplar axis) by falsification; that killing stands unrevisited. This decision adds no comparative axis: the ten dimensions still cite only the normative definition (glossary + §8.2 + DRs), and the reference is never a substitute standard. It proceeds through DR-CMD-011's G6 door as the contemplated new matter.

Stale-line resolution: the contract's Function line (introduced in 77bd530, predating DR-CMD-011) cited a "second exemplar" — a remnant DR-CMD-011's no-edits consequence left standing, in tension with the ratified no-exemplar decision. Amended by this disposition to the precise meaning: consultative worked reference; validation remains definition-anchored per DR-CMD-011.

## Decision

**Ratified O2 (contingent, contingency satisfied).** The dsys-mutation-playbook (`doc/dsys-mutation-playbook-spec.md`) is `/eval-pb`'s worked reference. `/eval-pb`'s contract and implementation are amended accordingly (consultative note only; no dimension changed). O3 stays deferred with its trigger.

## The `/eval-pb` report (instrument output, 2026-09-21)

```text
/eval-pb report
Candidate: doc/dsys-mutation-playbook-spec.md -> doc/dsys-mutation-playbook-spec.md @ b3058bb (working tree, clean)
Admission: admitted: claims the playbook form — DoD conditionals, rung options, G1–G4+M1–M4 gates, proposer/disposer separation, disposition production

Findings:
1. DoD conditionals: conforms — START/STOP/KEEP each When + Done when; ladder "advisory order, not a sequence"; re-entrant per I-4
2. Admission/kill gates: conforms — gates stated before evaluating with STOP routing; every kill names reason; merge + supersession branches present
3. Disposition modes: conforms — ratify/overrule/triage enumerated with DoDs; authorize/set_standing available; "same five modes, same DoDs" via shared §8.2 table (§8.4 pattern)
4. Proposer/disposer: conforms — "Unratified = draft = not a decision"; worked example disposes only on operator ratification
5. Records on disposition: conforms — DecisionRecord with playbook discriminator + I-17 mechanical closure; exactly-one-record per matter
6. Plane discipline: conforms — procedure the operator follows; no command-nodes; no embedded sequencing
7. Exercise declared: conforms — §6 worked example walks admit + kill paths; golden run 37 refusal cases PASS (§8.4)
8. Trigger declared: conforms — post-installation mutation of an installed dsys; non-trigger named (pre-installation developer mode, §9)
9. Output format: conforms — DecisionRecord field list specified (§4) + I-17 shape constraint; not per-run invention
10. Gate precision: conforms — every gate fail-able (M1 rung kills; patch→fork escalation; no-reason refused); F-M2 pre-registers a kill observation

Overall: conforms
```

## Consequences (G4)

`/eval-pb` gains a consultative reference; its procedure is otherwise unchanged (DR-CMD-015 intact). The stale "second exemplar" line is now precise. O3 (feature-expansion) remains a live future candidate under its trigger.

## Checkability (G5)

Binding. Checkable: the reference is named in both docs (or not); the `/eval-pb` run's conforms verdict is on record above; O3's trigger is observable (bootstrap completion).

## Uncertainties (G6)

- One-vs-two references later: if the feature-expansion playbook completes its bootstrap, does it join the mutation playbook as a second reference or replace it? Undecided — a new matter then.
- The reference's consultative status is a narrowing of DR-CMD-011's G6 door; if anyone later wants the comparative axis, that is a further new matter, not covered here.
- Numbering (disposition order): this ratification takes **DR-CMD-020**.
