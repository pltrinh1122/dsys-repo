# DR-CMD-024 — `/sc-author` triplet ratified

- **Status:** ratified
- **Date:** 2026-09-21 ~05:27 PDT
- **Selector:** Peter (operator; proposer = ambient agent — proposer≠disposer held)
- **Disposition mode:** ratify
- **Matter:** Resolve the `/sc-author` status contradiction — its
  "Status: active" headers conflicted with the standing rule that
  triplet artifacts need operator disposition before deployment and use.

## Options and gate trails (from `/pb-decide` START/STOP)

- **O1 — ratify the triplet:** survived; selected. The triplet is
  complete on the DR-CMD-006/010 schema: the contract binds the
  implementation (binding line present), the implementation names the
  design counterpart header-adjacent, files stay separate, specialized
  terms defined once in the design doc's glossary. The routine was
  exercised at creation (2026-09-20): it authored the `/eval-rb` and
  `/pb-extend` triplets. G1 ✓ (genuine matter — the contradiction was
  real), G2 ✓, G3 ✓ (no prior DR disposes `/sc-author`; DR-CMD-019
  records a gap, not a disposition), G4 ✓, G5 ✓ (status headers
  present/changed or not).
- **O2 — mark DRAFT-for-disposition:** survived as the honest
  alternative. Not selected: the triplet is complete and exercised,
  and the operator chose to dispose now rather than defer.
- **O3 — reframe / refuse:** the null. Not selected.

## Decision

**Ratified O1.** The `/sc-author` triplet (contract
`doc/slash-commands/sc-author.md`, `doc/sc-author-implementation.md`,
`doc/sc-author-design.md`) is ratified and deployed. "Status: active"
headers now cite this record. The design doc §7 provenance note
claiming "the creation order is the disposition" is struck (record
kept): the creation order was not the disposition — this record is.
The "Status: active" headers stood premature from 2026-09-20 until
this ratification.

## Consequences (G4)

`/sc-author` may now be invoked. Its staged output remains
DRAFT-for-disposition per its own procedure — ratifying the author
does not pre-ratify anything it authors.

## Checkability (G5)

Binding. Checkable: the implementation and design headers cite
DR-CMD-024 (or not); the §7 struck-record annotation is present
(or not).

## Uncertainties (G6)

- **Carried:** the DR-CMD-019 §7 gap (from the `/eval-meta`
  decomposed verdict) — record-and-defer, analysis in the design
  doc's G6, no procedure change. Revisit trigger: the first real
  `/sc-author` run.
- Numbering (disposition order): this ratification takes **DR-CMD-024**.
