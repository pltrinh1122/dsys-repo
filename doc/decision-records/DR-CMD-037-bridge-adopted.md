# DR-CMD-037 — Bridge (pydantic→core compile path) expansion ADOPTED with conditions

- **Status:** ratified (disposition)
- **Date:** 2026-09-21 ~06:49 PDT
- **Selector:** Peter (operator; proposer = ambient agent — proposer≠disposer held)
- **Disposition mode:** ratify (the feature-expansion playbook's §7 normal mode for expansion matters; authorize/set_standing/triage excluded)
- **Discriminator:** feature-expansion (shared machinery)
- **Matter:** the framed bridge claim — "dsys should gain a generic bridge — one compile path from pydantic automaton sources to AutomatonFlow/FlowRun + RunBook/Step/Tool entities — such that any future automaton is authored as pydantic models and executed on the existing FlowRun scheduler, with zero new engine code."

## Verdict trail (governed `/pb-extend` evaluation, this session)

- **Gates:** X1 clear (dual-scope, decomposed at START, conjunctive); X2 clear (R1 replay story stated and coherent — doesn't fail); X3 clear (pre-build; no implementation arriving); X4 clear (delta named and real: run-book structure compiled from source, not just states/transitions — the gap in `updater.compile_flow()`).
- **Conditionals holding:** A1 (F1/F2 genuine ambient attempts; F1 is the operator's own DR-CMD-030 objection in substance; survivors are specified constraints); A2 (one new term, justified against DR-CMD-030 vocabulary); A3 (core-side constructor, no plane crossing); A4 (the spec is the artifact); A5 (checkable sequences, §7); P1 (no CLI surface); P2 (pure function, no I/O); R1 (story stated); R4 (zero inference by construction); R5 (trust line declared, anti-smuggling rule specified).
- **Conditionals specified-but-unexecuted (build-time):** R2 (refusal cases §7.3–7.4, determinism §7.4 — specified, not run); R3 (B-1/B-2/B-3 specified, not implemented); P3 (docs converge on the build); P4 (zero new deps — verify import-clean at build).
- **Draft verdict:** adopt-with-conditions (four build-time conditions).

## Decision

**ADOPTED** — the operator's "Adopt with conditions": adoption under the verdict's four conditions:

1. Updater re-expression hash equality (spec §7.1) — the bridge subsumes `compile_flow()`.
2. Bridge golden run executed, including the refusal cases (spec §7.2–7.4).
3. B-1/B-2/B-3 implemented and run alongside I-14/I-15/I-16 over compiled output.
4. Docs converge on the build (P3).

Adopted bytes: `doc/bridge-spec.md` sha256 `96075ef6e8f4950771e79c25fe27d5d55d4e7c58f24c6c85912fa2fcefd1993f` (adopted header; working tree, uncommitted at disposition). G6 carried: operator falsification still available; module placement, migration sequencing, and second-automaton candidate stay open per spec §8. Next disposition identifier: DR-CMD-038.
