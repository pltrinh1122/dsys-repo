# DR-CMD-053 — acquisition contract spec ADOPTED

- **Status:** ratified (disposition)
- **Date:** 2026-09-21 ~13:36 PDT
- **Selector:** Peter (operator; proposer = ambient agent — proposer≠disposer held)
- **Disposition mode:** ratify
- **Matter:** *k1-q3b-handle-acquisition* — ADOPTED (narrowed, with conditions, DR-CMD-052) 2026-09-21. The adopted-now Y: *the bearer resolves the handle via accretion.path at initiation; absent path refuses; the identity lives as long as the installation.*

## Verdict trail

- **Lineage:** K1's G6 Q3 → DR-CMD-039 → K1 Q3 (DR-CMD-045/046/047; (a)-half adopted, spec'd, built) → DR-CMD-046 F4 → the production driver framed (DR-CMD-048), adopted (DR-CMD-049), spec'd and spec-adopted (DR-CMD-050), built and committed (`8b61695`) → the (b)-half framed (DR-CMD-051), evaluated (draft verdict: adopt-with-conditions, narrowed), adopted narrowed with conditions (DR-CMD-052) → the acquisition spec drafted 2026-09-21 (NBA O1).
- **S5 motivation-fit:** holds — the Z is a checkable world-claim, true on cited evidence (DR-CMD-046 F4's staging constraint discharged; the drive contract's settled boundary). Not misplaced, not fit-failing. Not INVALID.
- **Falsifiers:** run at evaluation (F1 narrowed — resolution is reading `accretion.path`; F2 killed rotation/revocation as designed operations; F3 narrowed; F4 killed — the tripwire holds; F5 narrowed, survives). No new falsifier at disposition.
- **Conditionals:** A1 (this evaluation; no refuted sub-claim survives — rotation/revocation and the handle-manager entity removed at adoption), A2 (no new entity — the spec is a contract: D1–D5, I-27), A3 (plane discipline — resolution is mechanical, process-side; the drive never resolves), A5 (the adopted Y is checkable — the R2 acceptances), R4 (zero inference — the resolver reads config and compares UUIDs), R5 (trust declared — the spec's Trust section). A4 satisfied (this spec is the A4-mandated artifact); R1 (replay), R2 (acceptances), R3 (I-27) defined as spec sections.

## Decision

**ADOPTED — the spec whole, no conditions.** The operator
selected "Adopt the spec." The disposition (initially
offered as "Adopt with conditions") was corrected to a
plain adoption at the operator's direction: no conditions
attached.

Adopted: D1 (resolution — the CLI surface reads the
install config's `accretion.path` at initiation, config-
contract precedence, opens `<path>/.git`, presents the
handle to the bearer; the drive never resolves); D2
(fail fast at resolution — the I-25 predicate applied
at initiation; D3's per-write check stays the backstop);
D3 (the failure-mode contract — absent path → refuse;
no manifest identity → refuse; identity mismatch →
fail fast; after refusal: no retry, record, surface);
D4 (the lifetime statement — identity lifetime =
installation lifetime; new identities only from fresh
installs; no rotation, no revocation); D5 (the K3 edge —
the resolver reads operator-owned config; the tripwire
holds); I-27 (authorized acquisition, R3); Replay (R1);
Trust declared (R5); Acceptances (R2); G6 residuals.

Adopted bytes: `doc/k1-q3b-acquisition-spec.md` sha256 `47dab54ea653ddab97cd5fee96ca0747acac356ec99850d9dcef08e9dfeaf9e1` (spec'd + adopted; working tree, uncommitted at disposition). Next disposition identifier: DR-CMD-054.

**Build authorization:** not granted by this record. The
build follows on the operator's separate direction. (The
G6 residuals name the unimplemented CLI flow-drive
surface as the enforcement home for I-27.)
